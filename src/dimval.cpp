
#include "dimval.hpp"

using namespace numeric;

time const numeric::sec = secs(1.0);
time const numeric::millisec = millisecs(1.0);
time const numeric::microsec = microsecs(1.0);
time const numeric::nanosec = nanosecs(1.0);
length const numeric::meter = meters(1.0);
length const numeric::kilometer = kilometers(1.0);
length const numeric::millimeter = millimeters(1.0);
length const numeric::micrometer = micrometers(1.0);
length const numeric::nanometer = nanometers(1.0);
length const numeric::angstrom = angstroms(1.0);
charge const numeric::coulomb = coulombs(1.0);
temperature const numeric::kelvin = kelvins(1.0);
force const numeric::newton = newtons(1.0);
force const numeric::dyne = dynes(1.0);
energy const numeric::joule = joules(1.0);
energy const numeric::erg = ergs(1.0);
power const numeric::watt = watts(1.0);
current const numeric::ampere = amperes(1.0);

