
#include "dimval.hpp"

using namespace num;

time const num::sec = secs(1.0);
time const num::millisec = millisecs(1.0);
time const num::microsec = microsecs(1.0);
time const num::nanosec = nanosecs(1.0);
length const num::meter = meters(1.0);
length const num::kilometer = kilometers(1.0);
length const num::millimeter = millimeters(1.0);
length const num::micrometer = micrometers(1.0);
length const num::nanometer = nanometers(1.0);
length const num::angstrom = angstroms(1.0);
charge const num::coulomb = coulombs(1.0);
temperature const num::kelvin = kelvins(1.0);
force const num::newton = newtons(1.0);
force const num::dyne = dynes(1.0);
energy const num::joule = joules(1.0);
energy const num::erg = ergs(1.0);
power const num::watt = watts(1.0);
current const num::ampere = amperes(1.0);

